# frozen_string_literal: true

source 'https://rubygems.org'

# Dependencies for e621archiver

# XMP metadata handling - replaces Image::ExifTool
gem 'mini_exiftool', '~> 1.5'

# Development/optional dependencies
group :development, :test do
  gem 'bundler', '~> 2.0'
end